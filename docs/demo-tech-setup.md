# Demo Technical Setup

The canonical, verified operator workflow is [Run the Alethea Demo from `main`](main-demo-operator-guide.md).

Use `bash scripts/setup_telegram_demo.sh` for the repeatable interactive setup. It follows the same synthetic-only ngrok workflow and does not install or stop operator-owned Ollama or OrbStack services.
